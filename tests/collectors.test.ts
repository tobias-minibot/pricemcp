import { afterEach,describe,expect,it,vi } from 'vitest';
import { readFileSync } from 'node:fs';
import { collectAmazon,collectApple,collectorInternals } from '../src/collectors.js';
import { collectBrightDataRetailers,extractBrightDataProduct,type BrightDataTarget } from '../src/brightdata.js';

const fixture=(name:string)=>readFileSync(new URL(`fixtures/${name}`,import.meta.url),'utf8');

afterEach(()=>vi.unstubAllGlobals());
describe('collector failure handling',()=>{
  it('records HTTP failures instead of fabricating observations',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response('blocked',{status:403})));const r=await collectAmazon();expect(r.status).toBe('failed');expect(r.observations).toHaveLength(0);expect(r.errors[0]).toContain('HTTP 403')});
  it('records malformed payloads as a failed run',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response('<html>'+('x'.repeat(600))+'</html>',{status:200})));const r=await collectApple();expect(r.status).toBe('failed');expect(r.errors[0]).toContain('bootstrap not found')});
  it('extracts only the primary new-condition Amazon buy box',()=>{const html=`<html><body><span id="productTitle">Apple 2026 MacBook Air 13-inch Laptop with M5 chip, 13.6-inch, 16GB Unified Memory, 512GB SSD</span><div id="corePriceDisplay_desktop_feature_div"><span id="apex-pricetopay-accessibility-label">$1,299.00</span></div><div id="sfsb_accordion_head">Ships from: Amazon.com Sold by: Amazon.com</div><input id="add-to-cart-button"></body></html>`;expect(collectorInternals.parseAmazonPdp(html)).toEqual({title:'Apple 2026 MacBook Air 13-inch Laptop with M5 chip, 13.6-inch, 16GB Unified Memory, 512GB SSD',raw_price:'$1,299.00',seller:'Amazon.com',ships_from:'Amazon.com',available:true})});
  it('rejects a third-party Amazon marketplace buy box',async()=>{const html=`<html><body>${'x'.repeat(600)}<span id="productTitle">Apple 2026 MacBook Air 13-inch Laptop with M5 chip, 13.6-inch, 16GB Unified Memory, 512GB SSD</span><div id="corePriceDisplay_desktop_feature_div"><span id="apex-pricetopay-accessibility-label">$999.00</span></div><div id="sfsb_accordion_head">Ships from: Amazon Sold by: Unknown Deals LLC</div><input id="add-to-cart-button"></body></html>`;vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(html,{status:200})));const r=await collectAmazon();expect(r.status).toBe('failed');expect(r.observations).toHaveLength(0);expect(r.errors[0]).toContain('Rejected non-Amazon seller')});
  it('keeps saved Apple and Best Buy source contracts readable',()=>{const apple=collectorInternals.parseAppleBootstrap(fixture('apple-product-selection.html'));expect(apple.products[0].btrOrFdPartNumber).toBe('TEST1');const bestBuy=collectorInternals.parseBestBuyTransport(fixture('bestbuy-search.html'));expect(bestBuy[0]).toMatchObject({skuId:'123',brand:'Apple'})});
  it('extracts schema.org product evidence from a Bright Data response',()=>{const target:BrightDataTarget={merchant_id:'example',merchant_name:'Example Electronics',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'TEST-SKU',accepted_sellers:['Example Electronics']};expect(extractBrightDataProduct(fixture('brightdata-product.html'),target)).toMatchObject({raw_price:'$1099.00',seller:'Example Electronics',available:true,extraction_path:'json-ld'})});
  it('fails closed when Bright Data returns an unapproved marketplace seller',async()=>{const html=fixture('brightdata-product.html').replace('Example Electronics','Unknown Marketplace LLC');vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(html,{status:200})));const target:BrightDataTarget={merchant_id:'walmart',merchant_name:'Walmart',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'TEST-SKU',accepted_sellers:['Example Electronics']};const result=await collectBrightDataRetailers([target],'token','zone');expect(result.status).toBe('failed');expect(result.observations).toHaveLength(0);expect(result.errors[0]).toContain('Rejected seller of record')});
  it('does not substitute product brand for missing seller evidence',()=>{const html=fixture('brightdata-product.html').replace(',"seller":{"@type":"Organization","name":"Example Electronics"}','').replace('"sku":"TEST-SKU"','"sku":"TEST-SKU","brand":{"@type":"Brand","name":"Apple"}');const target:BrightDataTarget={merchant_id:'walmart',merchant_name:'Walmart',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'TEST-SKU',accepted_sellers:['Apple']};expect(extractBrightDataProduct(html,target).seller).toBe('')});
  it('requires positive availability evidence',()=>{const html=fixture('brightdata-product.html').replace('"availability":"https://schema.org/InStock",','');const target:BrightDataTarget={merchant_id:'walmart',merchant_name:'Walmart',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'TEST-SKU',accepted_sellers:['Example Electronics']};expect(extractBrightDataProduct(html,target).available).toBe(false)});
  it('rejects unsupported currencies instead of parsing them as USD',async()=>{const html=fixture('brightdata-product.html').replace('"priceCurrency":"USD"','"priceCurrency":"EUR"');vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(html,{status:200})));const target:BrightDataTarget={merchant_id:'walmart',merchant_name:'Walmart',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'TEST-SKU',accepted_sellers:['Example Electronics']};const result=await collectBrightDataRetailers([target],'token','zone');expect(result.status).toBe('failed');expect(result.errors[0]).toContain('Unsupported currency: EUR')});
  it('requires page SKU evidence to match the configured retailer SKU',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(fixture('brightdata-product.html'),{status:200})));const target:BrightDataTarget={merchant_id:'walmart',merchant_name:'Walmart',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'OTHER-SKU',accepted_sellers:['Example Electronics']};const result=await collectBrightDataRetailers([target],'token','zone');expect(result.status).toBe('failed');expect(result.errors[0]).toContain('Rejected retailer SKU')});
  it('rejects unknown merchants before making a network request',async()=>{const fetchMock=vi.fn();vi.stubGlobal('fetch',fetchMock);const target:BrightDataTarget={merchant_id:'not-in-catalog',merchant_name:'Unknown',url:'https://example.test/p',expected_product_id:'apple-macbook-air-m5-13-16-512',source_product_id:'TEST-SKU',accepted_sellers:['Example Electronics']};const result=await collectBrightDataRetailers([target],'token','zone');expect(result.status).toBe('failed');expect(result.errors[0]).toContain('Unknown merchant');expect(fetchMock).not.toHaveBeenCalled()});
});
