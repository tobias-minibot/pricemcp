import type { PriceSearchResult } from './price-search.js';

const esc=(x:unknown)=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
const money=(minor:number|null|undefined)=>minor==null?'Unknown':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(minor/100);
const age=(seconds:number)=>seconds<60?`${seconds}s ago`:seconds<3600?`${Math.floor(seconds/60)}m ago`:seconds<86400?`${Math.floor(seconds/3600)}h ago`:`${Math.floor(seconds/86400)}d ago`;
const demo=()=>process.env.PRICEMCP_DATASET==='pricemcp-demo-v1';
const css=`:root{--ink:#101828;--muted:#667085;--line:#e4e7ec;--bg:#f7f9fc;--blue:#175cd3;--navy:#0b1220;--green:#067647;--amber:#b54708;--red:#b42318}*{box-sizing:border-box}body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;color:var(--ink);background:var(--bg)}a{color:var(--blue);text-decoration:none}nav{background:var(--navy);color:#fff;padding:18px 5vw;display:flex;justify-content:space-between;align-items:center}.brand{font-weight:800;letter-spacing:-.04em;font-size:22px}.brand em{color:#84adff;font-style:normal}nav a{color:#d0d5dd;margin-left:24px}.demo-banner{position:sticky;top:0;z-index:5;padding:11px 24px;text-align:center;background:#fffaeb;color:#7a2e0e;border-bottom:1px solid #fedf89;font-weight:800;letter-spacing:.04em}.wrap{max-width:1120px;margin:auto;padding:56px 24px}.hero{padding:72px 0 34px}.eyebrow{text-transform:uppercase;letter-spacing:.12em;color:var(--blue);font-weight:700;font-size:12px}h1{font-size:clamp(42px,7vw,76px);letter-spacing:-.06em;line-height:.98;margin:18px 0;max-width:900px}h2{font-size:30px;letter-spacing:-.035em;margin-top:42px}p.lead{font-size:20px;color:var(--muted);max-width:790px;line-height:1.55}.search{display:flex;gap:10px;margin:32px 0 18px;background:#fff;padding:8px;border:1px solid var(--line);border-radius:16px;box-shadow:0 12px 32px #10182812}.search input{flex:1;padding:18px;border:0;outline:0;font-size:18px;min-width:0}.btn{background:var(--blue);color:#fff;border:0;border-radius:10px;padding:0 26px;font-weight:700;cursor:pointer}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:16px}.card{display:block;background:#fff;border:1px solid var(--line);border-radius:14px;padding:22px;box-shadow:0 1px 2px #10182808}.card h3{margin:8px 0;font-size:18px}.flow{transition:transform .15s ease,border-color .15s ease}.flow:hover{transform:translateY(-2px);border-color:#84adff}.flow-number{font-size:12px;font-weight:800;color:var(--blue);text-transform:uppercase;letter-spacing:.08em}.muted{color:var(--muted)}.tag{display:inline-block;border-radius:99px;padding:5px 9px;font-size:11px;font-weight:800;letter-spacing:.06em;background:#ecfdf3;color:var(--green)}.soon{background:#f2f4f7;color:#667085}.demo-tag{background:#fffaeb;color:var(--amber)}.risk-tag{background:#fef3f2;color:var(--red)}.metric{font-size:30px;font-weight:800;letter-spacing:-.04em}.winner{border:2px solid #75e0a7;background:#f6fef9}.result-head{display:flex;justify-content:space-between;align-items:flex-end;gap:20px}.result-title{font-size:clamp(24px,5vw,38px);font-weight:800;letter-spacing:-.04em;overflow-wrap:anywhere}.reason{display:flex;flex-wrap:wrap;gap:8px;margin-top:16px}.offer-row{display:grid;grid-template-columns:1.5fr 1fr 1.4fr;gap:12px;align-items:center;padding:15px 0;border-bottom:1px solid var(--line)}.offer-row:last-child{border:0}.offers{width:100%;border-collapse:collapse;background:#fff;border-radius:14px;overflow:hidden}.offers th,.offers td{text-align:left;padding:15px;border-bottom:1px solid var(--line)}.offers th{font-size:12px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted)}code,pre{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}pre{background:#0b1220;color:#d0d5dd;padding:18px;border-radius:12px;overflow:auto;line-height:1.5}.notice{border-left:4px solid #f79009;background:#fffaeb;padding:15px 18px;border-radius:8px}.explain{border-left-color:#17b26a;background:#ecfdf3}.microcopy{font-size:14px;color:var(--muted);line-height:1.5}footer{padding:40px 5vw;background:#fff;border-top:1px solid var(--line);color:var(--muted)}@media(max-width:650px){.wrap{padding:34px 16px}.hero{padding:38px 0 18px}.search{flex-direction:column;padding:7px}.search input{font-size:16px}.btn{padding:16px}.result-head{align-items:flex-start;flex-direction:column}.result-title{font-size:22px;line-height:1.08}.offer-row{grid-template-columns:1fr auto}.offer-row .terms{grid-column:1/-1}.offers{font-size:13px}.offers th:nth-child(4),.offers td:nth-child(4){display:none}nav span:last-child{display:none}}`;
function shell(title:string,body:string){const banner=demo()?'<div class="demo-banner">DEMO DATA — SYNTHETIC PRICES — NOT LIVE OR PURCHASABLE</div>':'';return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${esc(title)}</title><style>${css}</style></head><body><nav><a class="brand" href="/">Price<em>MCP</em></a><span><a href="/#search">Prices</a><a href="/#developers">Developers</a><a href="/status">Status</a></span></nav>${banner}${body}<footer>PriceMCP prototype · Evidence before discounts · ${demo()?'All displayed offers are synthetic demo fixtures.':'Prices exclude location-dependent tax unless stated.'}</footer></body></html>`}
const resultPanel=(result:PriceSearchResult|null)=>{
  if(!result)return'';
  if(result.status!=='ok')return`<section id="results"><div class="notice"><strong>No trustworthy result.</strong> ${esc(result.error||result.ranking.explanation[0])}</div></section>`;
  const subject=result.subject as any,best=result.best_offer;
  const title=subject.type==='flight'?`${esc(subject.origin)} → ${esc(subject.destination)}`:esc(subject.name||subject.query);
  const subtitle=subject.type==='flight'?`${esc(subject.departure_date)}${subject.return_date?` → ${esc(subject.return_date)}`:''} · ${esc(subject.cabin)}`:'Exact canonical product match';
  const displayed=best?[best,...result.offers.filter(offer=>offer.offer_id!==best.offer_id)]:result.offers;
  const rows=displayed.slice(0,5).map((offer,index)=>`<div class="offer-row"><div><strong>${esc(offer.provider.name)}</strong>${index===0?'<br><span class="tag">BEST OPTION</span>':''}</div><div class="metric" style="font-size:24px">${money(offer.quote.total_minor)}</div><div class="terms muted">${esc(offer.quote.basis)}<br>${esc(offer.conditions.join(' · '))}</div></div>`).join('');
  const details=subject.type==='product'?`<a href="/products/${esc(subject.product_id)}">See full evidence and price history →</a>`:'';
  return`<section id="results"><div class="card winner"><div class="result-head"><div><div class="eyebrow">Best trustworthy option</div><div class="result-title">${title}</div><div class="muted">${subtitle}</div></div><div><div class="metric">${money(best?.quote.total_minor)}</div><div class="muted">${esc(best?.provider.name||'No eligible provider')}</div></div></div><div class="reason"><span class="tag">NO SPONSORED RANKING</span><span class="tag">AVAILABLE</span><span class="tag">SOURCE PRESERVED</span>${result.synthetic?'<span class="tag demo-tag">DEMO DATA</span>':''}</div><p class="microcopy">${esc(result.ranking.explanation.join(' '))}</p>${details}</div><div class="card" style="margin-top:16px">${rows}</div></section>`;
};
export function homePage(results:any[]=[],query='',priceResult:PriceSearchResult|null=null){
  const d=demo(),showResults=Boolean(query)&&!priceResult;
  const topScore=Number(results[0]?.score||0),visibleResults=query&&topScore>=1?results.filter(p=>Number(p.score)>=Math.max(.75,topScore-.3)):results;
  const cards=visibleResults.map((p,index)=>`<a class="card ${index===0?'winner':''}" href="/products/${esc(p.id)}"><span class="tag ${d?'demo-tag':p.active?'':'soon'}">${index===0&&query?'BEST MATCH':d?'SYNTHETIC':p.active?'CURRENT LINE':'REFERENCE'}</span><h3>${esc(p.name)}</h3><div class="muted">${esc(p.category)} · canonical SKU</div></a>`).join('');
  const judgeFlows=d?`<section aria-labelledby="judge-flows"><h2 id="judge-flows">Try the universal price layer</h2><div class="grid"><a class="card flow" href="/?q=Mac%20mini%20M4%2016GB%20256GB#results"><div class="flow-number">01 · Product</div><h3>I need a Mac mini.</h3><p class="muted">Exact variant, trusted merchants, transparent ranking.</p></a><a class="card flow" href="/?q=Flight%20Washington%20to%20Berlin#results"><div class="flow-number">02 · Flight</div><h3>Washington → Berlin</h3><p class="muted">The same price envelope with flight-specific conditions.</p></a><a class="card flow" href="/?q=iPhone%2017%20Pro%20256GB#results"><div class="flow-number">03 · Trust</div><h3>iPhone 17 Pro · 256GB</h3><p class="muted">See why a suspicious cheaper headline does not win.</p></a></div></section>`:'';
  return shell('PriceMCP — ask what it costs',`<main class="wrap"><section class="hero"><div class="eyebrow">No ads · no sponsored ranking · MCP-first</div><h1>What do you need?</h1><p class="lead">Ask what something costs. Get the best trustworthy options—with exact matches, fresh evidence, and a clear reason the winner ranks first.</p><form id="search" class="search" action="/" method="get"><input name="q" value="${esc(query)}" placeholder="Mac mini M4 or Flight Washington to Berlin" aria-label="What do you need?"><button class="btn">Search prices</button></form><p class="microcopy">Products and flights · one neutral price layer for people and AI agents</p></section>${resultPanel(priceResult)}${showResults?`<section id="results"><h2>Canonical matches for “${esc(query)}”</h2><div class="grid">${cards||'<div class="notice"><strong>No reliable match.</strong> PriceMCP will not substitute a nearby model or configuration.</div>'}</div></section>`:''}${judgeFlows}<h2>Why agents use PriceMCP</h2><div class="grid"><div class="card"><span class="tag">NORMALIZED</span><h3>Exact subjects, not keywords</h3><p class="muted">Product variants and flight itineraries retain their own conditions inside one quote envelope.</p></div><div class="card"><span class="tag">NEUTRAL</span><h3>Trust beats ad budgets</h3><p class="muted">Price, availability, freshness, match quality, and provider trust drive ranking—not sponsorship.</p></div><div class="card"><span class="tag">TRACEABLE</span><h3>Every quote keeps evidence</h3><p class="muted">Provider, source, currency, observed time, availability, and dependencies travel with the price.</p></div></div><section id="developers"><h2>One interface for every agent</h2><pre>search_price({ type: "product", query: "Mac mini M4 16GB 256GB" })\n\nsearch_price({\n  type: "flight", origin: "WAS", destination: "BER",\n  departure_date: "2026-09-18", return_date: "2026-09-25"\n})</pre><p class="microcopy">Existing product-specific tools remain available for compatibility. The universal primitive is the forward path for Products, Flights, and later FX.</p></section></main>`)
}
export function productPage(product:any,offers:any[],best:any,hist:any){
  const d=!!product.synthetic,winner=best.best_trusted_offer,cheapest=best.cheapest_offer;
  const rejected=cheapest&&winner&&cheapest.id!==winner.id&&!cheapest.trusted?cheapest:null;
  const rejectedReason=rejected?.marketplace_seller?'Unknown marketplace seller':rejected?.membership_required?'Membership dependency':'Insufficient seller trust';
  const sourceCell=(o:any)=>d?`<span class="tag demo-tag">SYNTHETIC RECORD</span><br><span class="muted">Observed ${esc(o.observed_at)}</span>`:`<a href="${esc(o.url)}" rel="nofollow noopener">Source ↗</a><br><span class="muted">Observed ${esc(o.observed_at)}</span>`;
  const winnerNote=winner?`<div class="notice explain"><strong>Why this wins:</strong> ${esc(winner.merchant_name)} is a verified, authorized seller with a fresh, new-condition, unconditional offer for this exact configuration.${rejected?` The ${money(rejected.total_minor)} headline from ${esc(rejected.merchant_name)} is cheaper, but excluded: ${esc(rejectedReason.toLowerCase())}.`:''}</div>`:'<div class="notice"><strong>No qualifying trusted winner.</strong> PriceMCP will not promote an unavailable, stale, membership-only, marketplace, or mismatched offer.</div>';
  return shell(`${product.name} — PriceMCP`,`<main class="wrap"><div class="eyebrow">Canonical ${esc(product.category)} · ${d?'controlled benchmark':product.active?'current':'reference configuration'}</div><h1 style="font-size:52px">${esc(product.name)}</h1>${d?'<div class="notice"><strong>Synthetic fixture.</strong> Product identity is canonicalized for interface testing; every price, merchant, timestamp, and history point on this page is mock data.</div>':!product.active?'<div class="notice">This configuration is retained for deterministic resolution but is no longer in the current new-product lineup. Only currently observed equivalent offers are shown.</div>':''}<div class="grid" style="margin-top:28px"><div class="card winner"><span class="tag">RECOMMENDED</span><div class="muted" style="margin-top:8px">Best trusted price</div><div class="metric">${money(winner?.total_minor)}</div><div class="muted">${esc(winner?.merchant_name||'No qualifying fresh offer')}</div></div>${rejected?`<div class="card"><span class="tag risk-tag">EXCLUDED</span><div class="muted" style="margin-top:8px">Cheapest headline</div><div class="metric">${money(rejected.total_minor)}</div><div class="muted">${esc(rejected.merchant_name)} · ${esc(rejectedReason)}</div></div>`:''}<div class="card"><div class="muted">Official reference</div><div class="metric">${money(best.official_price?.total_minor)}</div><div class="muted">${d?'Synthetic brand-direct reference':'Manufacturer direct'}</div></div><div class="card"><div class="muted">Savings vs official</div><div class="metric">${money(best.savings_vs_official_minor)}</div><div class="muted">Trusted, equivalent, unconditional offer</div></div><div class="card"><div class="muted">Member-only alternative</div><div class="metric">${money(best.best_membership_offer?.total_minor)}</div><div class="muted">Excluded unless membership is requested</div></div><div class="card"><div class="muted">30-day observed range</div><div class="metric">${money(hist.low_30d_minor)}–${money(hist.high_30d_minor)}</div><div class="muted">${esc(best.evidence_count)} current evidence records</div></div></div><div style="margin-top:18px">${winnerNote}</div><h2>Comparable offers</h2><p class="microcopy">All rows match the canonical specification below. Item price is shown; destination tax is excluded, and unknown shipping is called out rather than estimated.</p>${offers.length?`<table class="offers"><thead><tr><th>Merchant</th><th>Comparable price</th><th>Trust / terms</th><th>Freshness</th><th>Evidence</th></tr></thead><tbody>${offers.map(o=>`<tr><td><strong>${esc(o.merchant_name)}</strong><br><span class="muted">${esc(o.condition)}</span></td><td><strong>${money(o.total_minor)}</strong>${o.shipping_minor==null?'<br><span class="muted">item price · shipping unknown</span>':'<br><span class="muted">shipping included</span>'}</td><td>${o.trusted?'<span class="tag">TRUSTED</span>':'<span class="tag risk-tag">NOT TRUSTED</span>'}${o.membership_required?'<br><span class="tag demo-tag">MEMBERSHIP</span>':''}<br><span class="muted">trust score ${Number(o.trust_score).toFixed(2)}</span></td><td>${esc(o.freshness_status)}<br><span class="muted">${age(o.age_seconds)}</span>${!o.available?'<br><span class="tag soon">UNAVAILABLE</span>':''}</td><td>${sourceCell(o)}</td></tr>`).join('')}</tbody></table>`:'<div class="notice">No current equivalent offers are stored. PriceMCP does not substitute nearby configurations.</div>'}<h2>Canonical specification</h2><p class="microcopy">This identity is the comparison boundary: nearby chips, memory, storage, sizes, or editions are not interchangeable.</p><pre>${esc(JSON.stringify({id:product.id,dataset:product.dataset,synthetic:product.synthetic,attributes:product.attributes,official_url:product.official_url},null,2))}</pre></main>`)
}
export function statusPage(data:any){return shell('PriceMCP status',`<main class="wrap"><div class="eyebrow">Internal operations</div><h1 style="font-size:52px">${data.synthetic?'Demo dataset health':'Collection health'}</h1><p class="lead">Dataset ${esc(data.dataset)} · Checked ${esc(data.checked_at)} · Last refresh ${esc(data.last_refresh||'never')}</p><div class="grid">${Object.entries(data.counts).map(([k,v])=>`<div class="card"><div class="muted">${esc(k.replace(/_/g,' '))}</div><div class="metric">${esc(v)}</div></div>`).join('')}</div><h2>Collection runs</h2><pre>${esc(JSON.stringify(data.collector_runs,null,2))}</pre></main>`)}
