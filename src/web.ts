import type { PriceSearchResult } from './price-search.js';
import { webMcpClientScript } from './webmcp.js';

const esc=(x:unknown)=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
const money=(minor:number|null|undefined)=>minor==null?'Unknown':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(minor/100);
const age=(seconds:number)=>seconds<60?`${seconds}s ago`:seconds<3600?`${Math.floor(seconds/60)}m ago`:seconds<86400?`${Math.floor(seconds/3600)}h ago`:`${Math.floor(seconds/86400)}d ago`;
const demo=()=>process.env.PRICEMCP_DATASET==='pricemcp-demo-v1';
const css=`:root{--ink:#101828;--muted:#667085;--line:#e4e7ec;--bg:#f7f9fc;--blue:#175cd3;--navy:#0b1220;--green:#067647;--amber:#b54708;--red:#b42318}*{box-sizing:border-box}body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;color:var(--ink);background:var(--bg)}a{color:var(--blue);text-decoration:none}nav{background:var(--navy);color:#fff;padding:18px 5vw;display:flex;justify-content:space-between;align-items:center}.brand{font-weight:800;letter-spacing:-.04em;font-size:22px}.brand em{color:#84adff;font-style:normal}nav a{color:#d0d5dd;margin-left:24px}.demo-banner{position:sticky;top:0;z-index:5;padding:11px 24px;text-align:center;background:#fffaeb;color:#7a2e0e;border-bottom:1px solid #fedf89;font-weight:800;letter-spacing:.04em}.wrap{max-width:1120px;margin:auto;padding:56px 24px}.hero{padding:72px 0 34px}.eyebrow{text-transform:uppercase;letter-spacing:.12em;color:var(--blue);font-weight:700;font-size:12px}h1{font-size:clamp(42px,7vw,76px);letter-spacing:-.06em;line-height:.98;margin:18px 0;max-width:900px}h2{font-size:30px;letter-spacing:-.035em;margin-top:42px}p.lead{font-size:20px;color:var(--muted);max-width:790px;line-height:1.55}.search{display:flex;gap:10px;margin:32px 0 14px;background:#fff;padding:8px;border:1px solid var(--line);border-radius:16px;box-shadow:0 12px 32px #10182812}.search:focus-within{outline:3px solid #84adff;outline-offset:2px}.search input{flex:1;padding:18px;border:0;font-size:18px;min-width:0}.search input:focus{outline:0}.btn{display:inline-flex;align-items:center;justify-content:center;min-height:48px;background:var(--blue);color:#fff;border:0;border-radius:10px;padding:0 26px;font-weight:700;cursor:pointer}.btn.secondary{background:#fff;color:var(--ink);border:1px solid #475467}.btn.demo-choice{background:#fffaeb;color:#7a2e0e;border:1px solid #fedf89}.mcp-peek{margin:0 0 12px;background:#080d18;border:1px solid #243044;border-radius:14px;overflow:hidden;box-shadow:0 18px 42px #10182818}.mcp-peek-head{display:flex;justify-content:space-between;gap:16px;align-items:center;padding:11px 15px;border-bottom:1px solid #243044;color:#98a2b3;font:700 11px/1.2 ui-monospace,SFMono-Regular,Menlo,monospace;text-transform:uppercase;letter-spacing:.08em}.mcp-peek-title{display:flex;align-items:center;gap:9px;color:#d0d5dd}.mcp-dot{width:8px;height:8px;border-radius:50%;background:#12b76a;box-shadow:0 0 0 4px #12b76a20}.mcp-feed{height:180px;overflow:auto;padding:12px 15px;font:13px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;color:#98a2b3;scrollbar-color:#344054 transparent}.mcp-line{display:grid;grid-template-columns:72px 20px 1fr;gap:8px;animation:feed-in .2s ease-out}.mcp-time{color:#667085}.mcp-icon{color:#84adff}.mcp-line.ok .mcp-icon{color:#32d583}.mcp-line.warn .mcp-icon{color:#fdb022}.mcp-line.error .mcp-icon{color:#f97066}.mcp-line strong{color:#e4e7ec;font-weight:650}.mcp-cursor{display:inline-block;width:7px;height:14px;background:#84adff;vertical-align:-2px;animation:blink 1s steps(1) infinite}@keyframes feed-in{from{opacity:0;transform:translateY(4px)}}@keyframes blink{50%{opacity:0}}.partner-strip{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:10px 0 14px}.partner-chip{background:#fff;border:1px solid var(--line);border-radius:10px;padding:10px 12px}.partner-chip strong{display:block;font-size:13px}.partner-chip span{color:var(--muted);font-size:12px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:16px}.card{display:block;background:#fff;border:1px solid var(--line);border-radius:14px;padding:22px;box-shadow:0 1px 2px #10182808}.card h3{margin:8px 0;font-size:18px}.flow{transition:transform .15s ease,border-color .15s ease}.flow:hover{transform:translateY(-2px);border-color:#84adff}.flow-number{font-size:12px;font-weight:800;color:var(--blue);text-transform:uppercase;letter-spacing:.08em}.muted{color:var(--muted)}.tag{display:inline-block;border-radius:99px;padding:5px 9px;font-size:11px;font-weight:800;letter-spacing:.06em;background:#ecfdf3;color:var(--green)}.soon{background:#f2f4f7;color:#667085}.demo-tag{background:#fffaeb;color:var(--amber)}.risk-tag{background:#fef3f2;color:var(--red)}.metric{font-size:30px;font-weight:800;letter-spacing:-.04em}.winner{border:2px solid #75e0a7;background:#f6fef9}.result-head{display:flex;justify-content:space-between;align-items:flex-end;gap:20px}.result-title{font-size:clamp(24px,5vw,38px);font-weight:800;letter-spacing:-.04em;overflow-wrap:anywhere}.reason{display:flex;flex-wrap:wrap;gap:8px;margin-top:16px}.offer-row{display:grid;grid-template-columns:1.5fr 1fr 1.4fr;gap:12px;align-items:center;padding:15px 0;border-bottom:1px solid var(--line)}.offer-row:last-child{border:0}.offers{width:100%;border-collapse:collapse;background:#fff;border-radius:14px;overflow:hidden}.offers th,.offers td{text-align:left;padding:15px;border-bottom:1px solid var(--line)}.offers th{font-size:12px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted)}code,pre{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}pre{background:#0b1220;color:#d0d5dd;padding:18px;border-radius:12px;overflow:auto;line-height:1.5}.notice{border-left:4px solid #f79009;background:#fffaeb;padding:15px 18px;border-radius:8px}.explain{border-left-color:#17b26a;background:#ecfdf3}.microcopy{font-size:14px;color:var(--muted);line-height:1.5}.infra-callout{margin:34px 0;padding:28px;border-radius:18px;background:var(--navy);color:#fff;display:flex;gap:28px;align-items:center;justify-content:space-between}.infra-callout h2{margin:6px 0 10px}.infra-callout .muted{color:#98a2b3}.dev-actions{display:flex;flex-wrap:wrap;gap:10px;margin:28px 0}.dev-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.pipeline{grid-template-columns:repeat(4,1fr);margin:22px 0}.pipeline .card{min-height:150px}.pipeline .metric{font-size:24px;margin-top:12px;overflow-wrap:anywhere}.terminal{background:#0b1220;color:#d0d5dd;border-color:#344054}.terminal h2{color:#fff;margin-top:0}.terminal pre{padding:0;margin:0;max-height:430px}.run-state{display:flex;align-items:center;gap:9px;color:var(--muted);font-weight:700}.pulse{width:9px;height:9px;border-radius:50%;background:#12b76a;box-shadow:0 0 0 5px #12b76a22}.source-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px}.source-card{border-left:4px solid #17b26a}.source-card a{overflow-wrap:anywhere}.tool-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:12px}.tool-schema{font-size:12px;max-height:190px}.truth-note{background:#eff8ff;border-left:4px solid #2e90fa;padding:15px 18px;border-radius:8px}footer{padding:40px 5vw;background:#fff;border-top:1px solid var(--line);color:var(--muted)}@media(prefers-reduced-motion:reduce){.mcp-line,.mcp-cursor{animation:none}}@media(max-width:850px){.pipeline{grid-template-columns:1fr 1fr}.dev-grid{grid-template-columns:1fr}.infra-callout{align-items:flex-start;flex-direction:column}}@media(max-width:650px){.wrap{padding:34px 16px}.hero{padding:38px 0 18px}.search{flex-direction:column;padding:7px}.search input{font-size:16px}.btn{padding:14px 16px;width:100%}.mcp-peek-head{align-items:flex-start;flex-direction:column;gap:6px}.mcp-feed{height:190px;font-size:12px}.mcp-line{grid-template-columns:62px 16px 1fr;gap:6px}.partner-strip{grid-template-columns:1fr}.result-head{align-items:flex-start;flex-direction:column}.result-title{font-size:22px;line-height:1.08}.offer-row{grid-template-columns:1fr auto}.offer-row .terms{grid-column:1/-1}.offers{font-size:13px}.offers th:nth-child(4),.offers td:nth-child(4){display:none}.pipeline{grid-template-columns:1fr}nav span:last-child{display:none}}`;
function shell(title:string,body:string){const banner=demo()?'<div class="demo-banner">DEMO DATA — SYNTHETIC PRICES — NOT LIVE OR PURCHASABLE</div>':'';return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${esc(title)}</title><style>${css}</style></head><body><nav><a class="brand" href="/">Price<em>MCP</em></a><span><a href="/companion">Companion</a><a href="/#search">Prices</a><a href="/developer">Live infrastructure</a><a href="/status">Status</a></span></nav>${banner}${body}<footer>PriceMCP prototype · Evidence before discounts · ${demo()?'All displayed offers are synthetic demo fixtures.':'Prices exclude location-dependent tax unless stated.'}</footer>${webMcpClientScript}</body></html>`}
const resultPanel=(result:PriceSearchResult|null)=>{
  if(!result)return'';
  if(result.status!=='ok')return`<section id="results"><div class="notice"><strong>No trustworthy result.</strong> ${esc(result.error||result.ranking.explanation[0])}</div></section>`;
  const subject=result.subject as any,best=result.best_offer;
  const title=subject.type==='flight'?`${esc(subject.origin)} → ${esc(subject.destination)}`:esc(subject.name||subject.query);
  const subtitle=subject.type==='flight'?`${esc(subject.departure_date)}${subject.return_date?` → ${esc(subject.return_date)}`:''} · ${esc(subject.cabin)}`:'Exact canonical product match';
  const displayed=best?[best,...result.offers.filter(offer=>offer.offer_id!==best.offer_id)]:result.offers;
  const rows=displayed.slice(0,5).map((offer,index)=>`<div class="offer-row"><div><strong>${esc(offer.provider.name)}</strong>${index===0?'<br><span class="tag">BEST OPTION</span>':''}</div><div class="metric" style="font-size:24px">${money(offer.quote.total_minor)}</div><div class="terms muted">${esc(offer.quote.basis)}<br>${esc(offer.conditions.join(' · '))}</div></div>`).join('');
  const details=subject.type==='product'?`<a href="/products/${esc(subject.product_id)}">See full evidence and price history →</a>`:'';
  return`<section id="results"><div class="card winner"><div class="result-head"><div><div class="eyebrow">Best trustworthy option</div><div class="result-title">${title}</div><div class="muted">${subtitle}</div></div><div><div class="metric">${money(best?.quote.total_minor)}</div><div class="muted">${esc(best?.provider.name||'No eligible provider')}</div></div></div><div class="reason"><span class="tag">NO SPONSORED RANKING</span><span class="tag">AVAILABLE</span><span class="tag">SOURCE PRESERVED</span>${result.synthetic?'<span class="tag demo-tag">DEMO DATA</span>':''}</div><p class="microcopy">${esc(result.ranking.explanation.join(' '))}</p>${details}</div><div class="card" style="margin-top:16px">${rows}</div></section>`;
};
export function homePage(results:any[]=[],query='',priceResult:PriceSearchResult|null=null){
  const d=demo(),showResults=Boolean(query)&&!priceResult;
  const topScore=Number(results[0]?.score||0),visibleResults=query&&topScore>=1?results.filter(p=>Number(p.score)>=Math.max(.75,topScore-.3)):results;
  const cards=visibleResults.map((p,index)=>`<a class="card ${index===0?'winner':''}" href="/products/${esc(p.id)}"><span class="tag ${d?'demo-tag':p.active?'':'soon'}">${index===0&&query?'BEST MATCH':d?'SYNTHETIC':p.active?'CURRENT LINE':'REFERENCE'}</span><h3>${esc(p.name)}</h3><div class="muted">${esc(p.category)} · canonical SKU</div></a>`).join('');
  const judgeFlows=d?`<section aria-labelledby="judge-flows"><h2 id="judge-flows">Try the universal price layer</h2><div class="grid"><a class="card flow" href="/?q=Mac%20mini%20M4%2016GB%20256GB#results"><div class="flow-number">01 · Product</div><h3>I need a Mac mini.</h3><p class="muted">Exact variant, trusted merchants, transparent ranking.</p></a><a class="card flow" href="/?q=Flight%20Washington%20to%20Berlin%202026-09-18%202026-09-25#results"><div class="flow-number">02 · Flight</div><h3>Washington → Berlin</h3><p class="muted">Sep 18–25 · the same envelope with flight-specific conditions.</p></a><a class="card flow" href="/?q=iPhone%2017%20Pro%20256GB#results"><div class="flow-number">03 · Trust</div><h3>iPhone 17 Pro · 256GB</h3><p class="muted">See why a suspicious cheaper headline does not win.</p></a></div></section>`:'';
  return shell('PriceMCP — ask what it costs',`<main class="wrap"><section class="hero"><div class="eyebrow">No ads · no sponsored ranking · MCP-first</div><h1>What do you need?</h1><p class="lead">Ask what something costs. Get the best trustworthy options—with exact matches, fresh evidence, and a clear reason the winner ranks first.</p><form id="search" class="search" action="/" method="get"><input name="q" value="${esc(query)}" placeholder="Mac mini M4 or Flight WAS to BER 2026-09-18 2026-09-25" aria-label="What do you need?"><button class="btn">Search prices</button></form><section class="mcp-peek" aria-label="Live MCP activity"><div class="mcp-peek-head"><div class="mcp-peek-title"><span class="mcp-dot"></span>Live MCP + partner proof feed</div><span>${d?'synthetic judge dataset':'read-only timestamped snapshot'} · /mcp</span></div><div class="mcp-feed" id="mcp-feed" role="log" aria-live="polite" aria-relevant="additions"><div class="mcp-line"><span class="mcp-time">ready</span><span class="mcp-icon">›</span><span>Loading verified submission evidence <span class="mcp-cursor"></span></span></div></div></section><div class="partner-strip" aria-label="Hackathon partner integrations"><div class="partner-chip"><strong>Bright Data</strong><span>retailer evidence access</span></div><div class="partner-chip"><strong>TrueForge</strong><span>agent sandbox + approval</span></div><div class="partner-chip"><strong>Qodo</strong><span>independent code review</span></div></div><p class="microcopy">Recorded partner proof is labeled separately from live MCP query events. Products and flights share one neutral price layer.</p></section>${resultPanel(priceResult)}${showResults?`<section id="results"><h2>Canonical matches for “${esc(query)}”</h2><div class="grid">${cards||'<div class="notice"><strong>No reliable match.</strong> PriceMCP will not substitute a nearby model or configuration.</div>'}</div></section>`:''}${judgeFlows}<section class="infra-callout"><div><div class="eyebrow" style="color:#84adff">Live technical proof</div><h2>This is an MCP server, not a shopping-page backend.</h2><p class="muted">Invoke the real <code>search_price</code> tool, inspect its schema, watch entity resolution and source fan-in, then open the structured response with provenance intact.</p></div><a class="btn" href="/developer">Open infrastructure console →</a></section><h2>Why agents use PriceMCP</h2><div class="grid"><div class="card"><span class="tag">NORMALIZED</span><h3>Exact subjects, not keywords</h3><p class="muted">Product variants and flight itineraries retain their own conditions inside one quote envelope.</p></div><div class="card"><span class="tag">NEUTRAL</span><h3>Trust beats ad budgets</h3><p class="muted">Price, availability, freshness, match quality, and provider trust drive ranking—not sponsorship.</p></div><div class="card"><span class="tag">TRACEABLE</span><h3>Every quote keeps evidence</h3><p class="muted">Provider, source, currency, observed time, availability, and dependencies travel with the price.</p></div></div><section id="developers"><h2>One interface for every agent</h2><pre>search_price({ type: "product", query: "Mac mini M4 16GB 256GB" })\n\nsearch_price({\n  type: "flight", origin: "WAS", destination: "BER",\n  departure_date: "2026-09-18", return_date: "2026-09-25"\n})</pre><p class="microcopy">Existing product-specific tools remain available for compatibility. The universal primitive is the forward path for Products, Flights, and later FX.</p></section></main><script>
(()=>{
  const form=document.getElementById('search'),feed=document.getElementById('mcp-feed');
  if(!form||!feed)return;
  const stamp=()=>new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false});
  const add=(message,state='',icon='›')=>{const line=document.createElement('div');line.className='mcp-line '+state;const time=document.createElement('span');time.className='mcp-time';time.textContent=stamp();const mark=document.createElement('span');mark.className='mcp-icon';mark.textContent=icon;const text=document.createElement('span');text.textContent=message;line.append(time,mark,text);feed.appendChild(line);feed.scrollTop=feed.scrollHeight};
  const clear=()=>{while(feed.firstChild)feed.removeChild(feed.firstChild)};
  const dollars=value=>value==null?'unknown':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(value/100);
  const renderTrace=data=>{const trace=data.trace||{},response=data.response||{},offers=Array.isArray(response.offers)?response.offers:[];add('canonical identity · '+(trace.canonical_identity||'no exact match'),trace.canonical_identity?'ok':'warn',trace.canonical_identity?'✓':'!');add('source fan-in · '+(trace.provider_count||0)+' providers · '+((trace.providers||[]).join(' · ')||'none eligible'));offers.slice(0,5).forEach(offer=>add('offer normalized · '+(offer.provider&&offer.provider.name||'unknown')+' · '+dollars(offer.quote&&offer.quote.total_minor),'ok','+'));add((trace.normalized_offer_count||0)+' comparable offers ranked · '+(trace.duration_ms||0)+' ms','ok','✓');if(response.status!=='ok')add(response.error||(response.ranking&&response.ranking.explanation||[]).join(' ')||'No trustworthy result','warn','!')};
  try{const stored=sessionStorage.getItem('pricemcp-trace');if(stored){sessionStorage.removeItem('pricemcp-trace');clear();renderTrace(JSON.parse(stored));add('evidence-backed result rendered','ok','✓')}else{Promise.all([fetch('/v1/mcp/tools').then(response=>response.json()),fetch('/v1/hackathon-partners').then(response=>response.json())]).then(([tools,proof])=>{clear();add('LIVE · MCP transport ready · '+tools.endpoint,'ok','✓');add('LIVE · '+tools.tools.length+' read-only tools discovered · '+tools.duration_ms+' ms','ok','✓');(proof.partners||[]).forEach(partner=>add('VERIFIED RUN · '+partner.name+' · '+partner.facts.join(' · '),'ok','✓'));add('awaiting a live price query…')}).catch(()=>{clear();add('MCP trace unavailable · standard search remains ready','warn','!')})}}catch{}
  form.addEventListener('submit',async event=>{const input=form.querySelector('input[name=q]'),query=input&&input.value.trim();if(!query)return;event.preventDefault();clear();add('search_price request · '+query,'','→');add('resolving canonical subject and validating evidence…');const button=form.querySelector('button');if(button)button.disabled=true;try{const response=await fetch('/v1/mcp/search',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({query})});const data=await response.json();if(!response.ok)throw new Error(data.error||'MCP request failed');renderTrace(data);sessionStorage.setItem('pricemcp-trace',JSON.stringify(data));add('opening ranked result…','ok','→');setTimeout(()=>{location.href='/?q='+encodeURIComponent(query)+'#results'},550)}catch(error){add(error instanceof Error?error.message:'MCP request failed','error','×');add('falling back to standard server search…','warn','→');setTimeout(()=>form.submit(),500)}finally{if(button)button.disabled=false}});
})();
</script>`)
}

export function companionPage(options:{flightProviderConfigured?:boolean}={}){
  const providerState=options.flightProviderConfigured
    ?'<strong>Sandbox connected:</strong> searches use the configured flight provider through PriceMCP MCP. Test inventory is synthetic, not a real fare, and not bookable.'
    :'<strong>Provider not configured:</strong> this deployment will fail closed instead of substituting demo fares. Configure an authorized flight provider to search.';
  const initialTag=options.flightProviderConfigured?'SANDBOX READY':'NOT CONFIGURED';
  return shell('PriceMCP Companion — plan with evidence',`<style>
.companion{--violet:#6d5dfc;--night:#17152b;max-width:1240px}.companion-hero{margin:18px 0 22px;padding:48px;border-radius:30px;background:radial-gradient(circle at 86% 12%,#8b7fff 0,transparent 34%),linear-gradient(135deg,#17152b,#342d76);color:#fff;overflow:hidden}.companion-hero h1{font-size:clamp(42px,7vw,76px);max-width:760px;margin-bottom:14px}.companion-hero .lead{color:#d9d6ff}.concept{display:inline-flex;gap:8px;align-items:center;padding:8px 12px;border:1px solid #ffffff42;border-radius:999px;font-size:12px;font-weight:800;letter-spacing:.08em}.verticals,.filters{display:flex;gap:8px;flex-wrap:wrap}.verticals{margin:0 0 18px}.vertical{padding:10px 14px;border-radius:999px;border:1px solid var(--line);background:#fff;font-weight:750}.vertical.active{background:var(--night);color:#fff;border-color:var(--night)}.vertical:disabled{color:#98a2b3}.trip-form{display:grid;grid-template-columns:1fr 1fr 1fr 1fr auto;gap:10px;margin-top:30px;padding:10px;background:#fff;border-radius:18px;color:var(--ink);box-shadow:0 24px 70px #09071966}.trip-form label{padding:8px 10px;font-size:11px;color:var(--muted);font-weight:800;text-transform:uppercase;letter-spacing:.05em}.trip-form input{display:block;width:100%;border:0;padding:8px 0 2px;font:700 16px/1.2 inherit;color:var(--ink);background:transparent}.trip-form input:focus{outline:0}.trip-form button{border-radius:12px;background:var(--violet);min-width:132px}.filters{margin:14px 0 20px}.filter{display:flex;align-items:center;gap:7px;padding:9px 12px;border:1px solid var(--line);background:#fff;border-radius:10px;font-size:13px;font-weight:700}.filter input{accent-color:var(--violet)}.filter select{border:0;background:transparent;font:inherit}.companion-layout{display:grid;grid-template-columns:1.55fr .75fr;gap:18px}.offer-stack{display:grid;gap:12px}.trip-card{display:grid;grid-template-columns:1fr auto;gap:16px;padding:22px;border:1px solid var(--line);background:#fff;border-radius:20px;box-shadow:0 8px 28px #1018280a}.trip-card.best{border:2px solid #8b7fff;box-shadow:0 16px 44px #6d5dfc1f}.carrier{font-size:21px;font-weight:850}.price{font-size:34px;font-weight:850;letter-spacing:-.05em;text-align:right}.facts{display:flex;flex-wrap:wrap;gap:7px;margin-top:14px}.fact{padding:6px 9px;background:#f2f4f7;border-radius:8px;font-size:12px;color:#475467}.fact.good{background:#ecfdf3;color:#067647}.card-actions{display:flex;gap:8px;margin-top:15px}.save{border:1px solid #d0d5dd;background:#fff;border-radius:9px;padding:8px 11px;font-weight:750;cursor:pointer}.save.saved{background:#f4f3ff;color:#5925dc;border-color:#bdb4fe}.aside{position:sticky;top:18px;height:max-content}.evidence-score{display:flex;align-items:center;justify-content:space-between;padding-bottom:16px;border-bottom:1px solid var(--line)}.score{width:58px;height:58px;display:grid;place-items:center;border-radius:50%;background:#ecfdf3;color:#067647;font-weight:900}.timeline{margin:18px 0;padding-left:18px;border-left:2px solid #d9d6ff}.timeline div{margin:0 0 18px}.shortlist{margin:16px 0;padding-top:14px;border-top:1px solid var(--line)}.shortlist ul{padding-left:18px}.empty{padding:42px;text-align:center;border:1px dashed #98a2b3;border-radius:20px;color:var(--muted)}@media(max-width:900px){.trip-form{grid-template-columns:1fr 1fr}.trip-form button{grid-column:1/-1}.companion-layout{grid-template-columns:1fr}.aside{position:static}}@media(max-width:560px){.companion-hero{padding:28px 20px}.trip-form{grid-template-columns:1fr}.trip-form button{grid-column:auto}.trip-card{grid-template-columns:1fr}.price{text-align:left}}
</style><main class="wrap companion"><div class="verticals" aria-label="Companion catalog"><button class="vertical active">Flights</button><button class="vertical" disabled title="Contract ready; awaiting approved inventory">Stays · next</button><button class="vertical" disabled>Cars</button><button class="vertical" disabled>Activities</button><a class="vertical" href="/v1/catalog">View catalog API</a></div><section class="companion-hero"><div class="concept">PRICE MCP COMPANION · MCP-BACKED PREVIEW</div><h1>A trip you can actually explain.</h1><p class="lead">Compare the real trade-offs—not just the cheapest headline. Change a constraint, inspect the evidence, save a shortlist, and stay in control of the handoff.</p><form class="trip-form" id="trip-form"><label>From<input name="origin" value="JFK" maxlength="3" required aria-label="Origin airport"></label><label>To<input name="destination" value="LHR" maxlength="3" required aria-label="Destination airport"></label><label>Depart<input name="departure" type="date" value="2026-10-10" required></label><label>Return<input name="return" type="date" value="2026-10-17" required></label><button class="btn">Compare trips</button></form></section><div class="notice" id="provider-state">${providerState}</div><div class="filters" aria-label="Comparison constraints"><label class="filter"><input id="nonstop" type="checkbox"> Nonstop only</label><label class="filter">Maximum total <select id="budget"><option value="0">Any price</option><option value="50000">$500</option><option value="75000">$750</option><option value="100000">$1,000</option><option value="150000">$1,500</option></select></label><span class="filter">1 adult · Economy</span></div><section class="companion-layout"><div><div class="result-head"><div><div class="eyebrow">Evidence-ranked options</div><h2 style="margin:6px 0 18px" id="route-title">JFK → LHR</h2></div><span class="tag demo-tag" id="dataset-tag">${initialTag}</span></div><div id="trip-results" class="offer-stack"><div class="empty">Choose “Compare trips” to run the same PriceMCP search used by MCP clients.</div></div></div><aside class="card aside"><div class="evidence-score"><div><div class="eyebrow">Decision quality</div><strong id="evidence-label">Waiting</strong></div><div class="score" id="evidence-score">—</div></div><div class="timeline"><div><strong>1 · Exact itinerary</strong><br><span class="microcopy">Dates, route, cabin, and travelers stay explicit.</span></div><div><strong>2 · Comparable total</strong><br><span class="microcopy">Conditions, source, observation time, and expiry travel with every quote.</span></div><div><strong>3 · Human handoff</strong><br><span class="microcopy">Review first; provider checkout only when a verified offer-specific link exists.</span></div></div><div class="shortlist"><strong>Saved shortlist</strong><ul id="shortlist"><li class="microcopy">Nothing saved yet.</li></ul></div><button class="btn secondary" style="width:100%" disabled>Continue with provider</button><p class="microcopy">Booking remains disabled until PriceMCP has verified live inventory and an offer-specific checkout link.</p></aside></section></main><script>
const tripForm=document.getElementById('trip-form'),tripResults=document.getElementById('trip-results');
const tripMoney=(value,currency)=>new Intl.NumberFormat('en-US',{style:'currency',currency:currency||'USD',maximumFractionDigits:0}).format(value/100);
const safeText=value=>String(value==null?'':value);
let latest=null;const saved=new Map(JSON.parse(localStorage.getItem('pricemcp-shortlist')||'[]'));
function persist(){localStorage.setItem('pricemcp-shortlist',JSON.stringify([...saved]));const list=document.getElementById('shortlist');while(list.firstChild)list.removeChild(list.firstChild);if(!saved.size){const li=document.createElement('li');li.className='microcopy';li.textContent='Nothing saved yet.';list.appendChild(li);return}saved.forEach((label,id)=>{const li=document.createElement('li');li.textContent=label;list.appendChild(li)})}
function filteredOffers(){const offers=(latest&&latest.response&&latest.response.offers)||[],budget=Number(document.getElementById('budget').value),nonstop=document.getElementById('nonstop').checked;return offers.filter(offer=>(!budget||offer.quote.total_minor<=budget)&&(!nonstop||(offer.conditions||[]).some(value=>/^nonstop/i.test(value)||/^0 total stop/i.test(value))))}
function renderTrips(){while(tripResults.firstChild)tripResults.removeChild(tripResults.firstChild);const offers=filteredOffers(),visible=offers.slice(0,24),result=latest&&latest.response,subject=result&&result.subject;if(subject)document.getElementById('route-title').textContent=safeText(subject.origin)+' → '+safeText(subject.destination);const tag=document.getElementById('dataset-tag');if(result){tag.textContent=result.synthetic?'SANDBOX TEST':'LIVE';document.getElementById('provider-state').innerHTML=result.synthetic?'<strong>Sandbox result:</strong> provider-backed test inventory; synthetic, not a real fare, and not bookable.':'<strong>Live evidence:</strong> verify expiry, fare rules, and checkout availability before acting.'}if(!offers.length){const empty=document.createElement('div');empty.className='empty';empty.textContent=latest?'No offer satisfies these constraints. Relax a filter to compare the source offers.':'Choose “Compare trips” to search.';tripResults.appendChild(empty)}visible.forEach((offer,index)=>{const card=document.createElement('article');card.className='trip-card '+(index===0?'best':'');const left=document.createElement('div'),right=document.createElement('div');const carrier=document.createElement('div');carrier.className='carrier';carrier.textContent=offer.provider.name;const route=document.createElement('div');route.className='muted';route.textContent=safeText(subject.origin)+' → '+safeText(subject.destination)+' · '+safeText(subject.cabin);const facts=document.createElement('div');facts.className='facts';(offer.conditions||[]).forEach(value=>{const fact=document.createElement('span');fact.className='fact';fact.textContent=value;facts.appendChild(fact)});[safeText(offer.source&&offer.source.method),offer.observed_at?'Observed '+new Date(offer.observed_at).toLocaleString():'Observation time unknown',offer.expires_at?'Expires '+new Date(offer.expires_at).toLocaleString():'Expiry unknown'].forEach(value=>{const fact=document.createElement('span');fact.className='fact';fact.textContent=value;facts.appendChild(fact)});const proof=document.createElement('span');proof.className='fact good';proof.textContent=index===0?'Best under your constraints':'Comparable option';facts.appendChild(proof);const actions=document.createElement('div');actions.className='card-actions';const save=document.createElement('button');save.className='save '+(saved.has(offer.offer_id)?'saved':'');save.textContent=saved.has(offer.offer_id)?'✓ Saved':'＋ Shortlist';save.addEventListener('click',()=>{if(saved.has(offer.offer_id))saved.delete(offer.offer_id);else saved.set(offer.offer_id,offer.provider.name+' · '+tripMoney(offer.quote.total_minor,offer.quote.currency));persist();renderTrips()});actions.appendChild(save);left.append(carrier,route,facts,actions);const amount=document.createElement('div');amount.className='price';amount.textContent=tripMoney(offer.quote.total_minor,offer.quote.currency);const basis=document.createElement('div');basis.className='microcopy';basis.textContent=offer.quote.basis;right.append(amount,basis);card.append(left,right);tripResults.appendChild(card)});document.getElementById('evidence-label').textContent=offers.length?offers.length+' comparable; showing '+visible.length:'No eligible result';document.getElementById('evidence-score').textContent=offers.length?'3/3':'0/3'}
document.getElementById('nonstop').addEventListener('change',renderTrips);document.getElementById('budget').addEventListener('change',renderTrips);persist();
tripForm.addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(tripForm),button=tripForm.querySelector('button');button.disabled=true;button.textContent='Comparing…';try{const response=await fetch('/v1/mcp/invoke',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({tool:'search_price',arguments:{type:'flight',origin:String(form.get('origin')).toUpperCase(),destination:String(form.get('destination')).toUpperCase(),departure_date:form.get('departure'),return_date:form.get('return'),cabin:'economy',adults:1}})});const data=await response.json(),result=data&&data.response;if(!response.ok)throw new Error(data.error||data.message||'Search failed');if(!result||result.status!=='ok')throw new Error(result&&result.error||(result&&result.ranking&&result.ranking.explanation&&result.ranking.explanation[0])||'No provider-backed offers returned');latest=data;renderTrips()}catch(error){while(tripResults.firstChild)tripResults.removeChild(tripResults.firstChild);const empty=document.createElement('div');empty.className='empty';empty.textContent=error instanceof Error?error.message:'Search failed';tripResults.appendChild(empty);document.getElementById('evidence-label').textContent='No verified result';document.getElementById('evidence-score').textContent='0/3'}finally{button.disabled=false;button.textContent='Compare trips'}});
</script>`)
}

export function developerPage(){
  return shell('PriceMCP — live infrastructure console',`<main class="wrap"><section class="hero"><div class="eyebrow">Live MCP infrastructure console</div><h1 style="font-size:clamp(42px,6vw,68px)">Watch the price layer work.</h1><p class="lead">This console uses an official MCP SDK client to invoke the same PriceMCP server exposed at <code>/mcp</code>. It shows the request, entity resolution, source fan-in, normalization, ranking, provenance, and structured response.</p><div class="dev-actions"><button class="btn" id="run-product">Run live 5-store product query</button><button class="btn demo-choice" id="run-flight">Run disclosed flight fixture</button><a class="btn secondary" href="/v1/mcp/tools">Open tools/list JSON</a></div><div class="run-state"><span class="pulse"></span><span id="run-state">Loading the real MCP tool schema…</span></div></section><div class="truth-note"><strong>Truth boundary:</strong> Product evidence is the deployed live snapshot. The flight example is explicitly synthetic and non-bookable. If a live provider is not configured, PriceMCP fails closed instead of inventing a fare.</div><section><h2>Pipeline trace</h2><div class="grid pipeline"><div class="card"><div class="flow-number">01 · Protocol</div><div class="metric" id="stage-protocol">MCP</div><p class="microcopy">Official SDK client → PriceMCP tool server</p></div><div class="card"><div class="flow-number">02 · Entity</div><div class="metric" id="stage-entity">—</div><p class="microcopy">Canonical product or complete itinerary</p></div><div class="card"><div class="flow-number">03 · Source fan-in</div><div class="metric" id="stage-sources">—</div><p class="microcopy" id="stage-providers">Waiting for invocation</p></div><div class="card"><div class="flow-number">04 · Normalize + rank</div><div class="metric" id="stage-latency">—</div><p class="microcopy" id="stage-ranking">Comparable quote envelope</p></div></div></section><section class="dev-grid"><div class="card terminal"><h2>Actual MCP request</h2><pre id="request-json">Waiting…</pre></div><div class="card terminal"><h2>Structured MCP response</h2><pre id="response-json">Waiting…</pre></div></section><section><h2>Normalized offers + provenance</h2><div class="source-grid" id="source-grid"><div class="card">Run a query to inspect source-backed offers.</div></div></section><section><h2>Live MCP tool schema</h2><p class="microcopy">Loaded from <code>tools/list</code> against the real read-only server—not copied into this page.</p><div class="tool-list" id="tool-list"></div></section></main><script>
const byId=id=>document.getElementById(id);
const pretty=value=>JSON.stringify(value,null,2);
const money=value=>value==null?'Unknown':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(value/100);
const clear=node=>{while(node.firstChild)node.removeChild(node.firstChild)};
const safeSourceUrl=value=>{try{const url=new URL(value);return url.protocol==='https:'?url.href:null}catch{return null}};
async function loadTools(){
  const response=await fetch('/v1/mcp/tools');
  if(!response.ok)throw new Error('tools/list failed with HTTP '+response.status);
  const data=await response.json();
  const list=byId('tool-list');clear(list);
  data.tools.forEach(tool=>{
    const card=document.createElement('div');card.className='card';
    const name=document.createElement('h3');name.textContent=tool.name;
    const description=document.createElement('p');description.className='microcopy';description.textContent=tool.description||'No description';
    const schema=document.createElement('pre');schema.className='tool-schema';schema.textContent=pretty(tool.inputSchema);
    card.append(name,description,schema);list.appendChild(card);
  });
  byId('run-state').textContent=data.tools.length+' read-only MCP tools discovered in '+data.duration_ms+' ms';
}
function renderOffers(data){
  const grid=byId('source-grid');clear(grid);
  const offers=Array.isArray(data.response&&data.response.offers)?data.response.offers:[];
  if(!offers.length){const empty=document.createElement('div');empty.className='notice';empty.textContent='No offers returned. The response above explains the fail-closed reason.';grid.appendChild(empty);return}
  offers.forEach(offer=>{
    const card=document.createElement('div');card.className='card source-card';
    const provider=document.createElement('h3');provider.textContent=offer.provider&&offer.provider.name||'Unknown provider';
    const quote=document.createElement('div');quote.className='metric';quote.textContent=money(offer.quote&&offer.quote.total_minor);
    const facts=document.createElement('p');facts.className='microcopy';facts.textContent=(offer.provider&&offer.provider.trusted?'trusted':'not trusted')+' · '+(offer.freshness&&offer.freshness.status||'no freshness')+' · match '+(offer.match&&offer.match.confidence!=null?offer.match.confidence:'unknown')+' · observed '+(offer.observed_at||'unknown');
    card.append(provider,quote,facts);
    const sourceUrl=safeSourceUrl(offer.source&&offer.source.url);
    if(sourceUrl){const link=document.createElement('a');link.href=sourceUrl;link.target='_blank';link.rel='nofollow noopener';link.textContent=(offer.source.method||'source')+' →';card.appendChild(link)}else{const source=document.createElement('span');source.className='microcopy';source.textContent=(offer.source&&offer.source.method||'source')+' · no safe public URL';card.appendChild(source)}
    grid.appendChild(card);
  });
}
function render(data){
  byId('request-json').textContent='search_price('+pretty(data.request)+')';
  byId('response-json').textContent=pretty(data.response);
  byId('stage-protocol').textContent=data.protocol;
  byId('stage-entity').textContent=data.trace.canonical_identity||'No match';
  byId('stage-sources').textContent=data.trace.provider_count+' providers';
  byId('stage-providers').textContent=data.trace.providers.join(' · ')||'No eligible provider';
  byId('stage-latency').textContent=data.trace.duration_ms+' ms';
  byId('stage-ranking').textContent=data.trace.normalized_offer_count+' normalized offers · '+(data.trace.ranking_policy||'fail closed');
  byId('run-state').textContent=(data.mode==='live_snapshot'?'LIVE SNAPSHOT':'SYNTHETIC / NOT BOOKABLE')+' · '+data.trace.provenance_urls+'/'+data.trace.normalized_offer_count+' offers retain source URLs';
  renderOffers(data);
}
async function invoke(payload){
  byId('run-state').textContent='Calling the PriceMCP MCP server…';
  const response=await fetch('/v1/mcp/invoke',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});
  const data=await response.json();
  if(!response.ok)throw new Error(data.error||'MCP invocation failed');
  render(data);
}
const product={tool:'search_price',arguments:{type:'product',query:'AirPods Pro 3'}};
const flight={tool:'search_price',demo:true,arguments:{type:'flight',origin:'WAS',destination:'BER',departure_date:'2026-09-18',return_date:'2026-09-25',cabin:'economy',adults:1}};
byId('run-product').addEventListener('click',()=>invoke(product).catch(showError));
byId('run-flight').addEventListener('click',()=>invoke(flight).catch(showError));
function showError(error){byId('run-state').textContent='Error: '+error.message}
loadTools().then(()=>invoke(product)).catch(showError);
</script>`)
}
export function productPage(product:any,offers:any[],best:any,hist:any){
  const d=!!product.synthetic,winner=best.best_trusted_offer,cheapest=best.cheapest_offer;
  const rejected=cheapest&&winner&&cheapest.id!==winner.id&&!cheapest.trusted?cheapest:null;
  const rejectedReason=rejected?.marketplace_seller?'Unknown marketplace seller':rejected?.membership_required?'Membership dependency':'Insufficient seller trust';
  const sourceCell=(o:any)=>d?`<span class="tag demo-tag">SYNTHETIC RECORD</span><br><span class="muted">Observed ${esc(o.observed_at)}</span>`:`<a href="${esc(o.url)}" rel="nofollow noopener">Source ↗</a><br><span class="muted">Observed ${esc(o.observed_at)}</span>`;
  const winnerNote=winner?`<div class="notice explain"><strong>Why this wins:</strong> ${esc(winner.merchant_name)} is a verified, authorized seller with a fresh, new-condition, unconditional offer for this exact configuration.${rejected?` The ${money(rejected.total_minor)} headline from ${esc(rejected.merchant_name)} is cheaper, but excluded: ${esc(rejectedReason.toLowerCase())}.`:''}</div>`:'<div class="notice"><strong>No qualifying trusted winner.</strong> PriceMCP will not promote an unavailable, stale, membership-only, marketplace, or mismatched offer.</div>';
  return shell(`${product.name} — PriceMCP`,`<main class="wrap"><div class="eyebrow">Canonical ${esc(product.category)} · ${d?'controlled benchmark':product.active?'current':'reference configuration'}</div><h1 style="font-size:52px">${esc(product.name)}</h1>${d?'<div class="notice"><strong>Synthetic fixture.</strong> Product identity is canonicalized for interface testing; every price, merchant, timestamp, and history point on this page is mock data.</div>':!product.active?'<div class="notice">This configuration is retained for deterministic resolution but is no longer in the current new-product lineup. Only currently observed equivalent offers are shown.</div>':''}<div class="grid" style="margin-top:28px"><div class="card winner"><span class="tag">RECOMMENDED</span><div class="muted" style="margin-top:8px">Best trusted price</div><div class="metric">${money(winner?.total_minor)}</div><div class="muted">${esc(winner?.merchant_name||'No qualifying fresh offer')}</div></div>${rejected?`<div class="card"><span class="tag risk-tag">EXCLUDED</span><div class="muted" style="margin-top:8px">Cheapest headline</div><div class="metric">${money(rejected.total_minor)}</div><div class="muted">${esc(rejected.merchant_name)} · ${esc(rejectedReason)}</div></div>`:''}<div class="card"><div class="muted">Official reference</div><div class="metric">${money(best.official_price?.total_minor)}</div><div class="muted">${d?'Synthetic brand-direct reference':'Manufacturer direct'}</div></div><div class="card"><div class="muted">Savings vs official</div><div class="metric">${money(best.savings_vs_official_minor)}</div><div class="muted">Trusted, equivalent, unconditional offer</div></div><div class="card"><div class="muted">Member-only alternative</div><div class="metric">${money(best.best_membership_offer?.total_minor)}</div><div class="muted">Excluded unless membership is requested</div></div><div class="card"><div class="muted">30-day observed range</div><div class="metric">${money(hist.low_30d_minor)}–${money(hist.high_30d_minor)}</div><div class="muted">${esc(best.evidence_count)} current evidence records</div></div></div><div style="margin-top:18px">${winnerNote}</div><h2>Comparable offers</h2><p class="microcopy">All rows match the canonical specification below. Item price is shown; destination tax is excluded, and unknown shipping is called out rather than estimated.</p>${offers.length?`<table class="offers"><thead><tr><th>Merchant</th><th>Comparable price</th><th>Trust / terms</th><th>Freshness</th><th>Evidence</th></tr></thead><tbody>${offers.map(o=>`<tr><td><strong>${esc(o.merchant_name)}</strong><br><span class="muted">${esc(o.condition)}</span></td><td><strong>${money(o.total_minor)}</strong>${o.shipping_minor==null?'<br><span class="muted">item price · shipping unknown</span>':'<br><span class="muted">shipping included</span>'}</td><td>${o.trusted?'<span class="tag">TRUSTED</span>':'<span class="tag risk-tag">NOT TRUSTED</span>'}${o.membership_required?'<br><span class="tag demo-tag">MEMBERSHIP</span>':''}<br><span class="muted">trust score ${Number(o.trust_score).toFixed(2)}</span></td><td>${esc(o.freshness_status)}<br><span class="muted">${age(o.age_seconds)}</span>${!o.available?'<br><span class="tag soon">UNAVAILABLE</span>':''}</td><td>${sourceCell(o)}</td></tr>`).join('')}</tbody></table>`:'<div class="notice">No current equivalent offers are stored. PriceMCP does not substitute nearby configurations.</div>'}<h2>Canonical specification</h2><p class="microcopy">This identity is the comparison boundary: nearby chips, memory, storage, sizes, or editions are not interchangeable.</p><pre>${esc(JSON.stringify({id:product.id,dataset:product.dataset,synthetic:product.synthetic,attributes:product.attributes,official_url:product.official_url},null,2))}</pre></main>`)
}
export function statusPage(data:any){return shell('PriceMCP status',`<main class="wrap"><div class="eyebrow">Internal operations</div><h1 style="font-size:52px">${data.synthetic?'Demo dataset health':'Collection health'}</h1><p class="lead">Dataset ${esc(data.dataset)} · Checked ${esc(data.checked_at)} · Last refresh ${esc(data.last_refresh||'never')}</p><div class="grid">${Object.entries(data.counts).map(([k,v])=>`<div class="card"><div class="muted">${esc(k.replace(/_/g,' '))}</div><div class="metric">${esc(v)}</div></div>`).join('')}</div><h2>Collection runs</h2><pre>${esc(JSON.stringify(data.collector_runs,null,2))}</pre></main>`)}
